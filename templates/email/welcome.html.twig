<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{ email.subject }}</title>
</head>
<body>
<div class="container">
    <p>Welcome to our family {{ email.toName }} !</p>
    <footer>
        <a href="{{ url('home') }}"><img src="{{ absolute_url(asset('images/icon Jakpaa.png')) }}"
                                         alt="Logo Jakpaa"></a><br/>
        <a href="{{ url('home') }}"><button class="btn-success">Jakpaa</button></a>
    </footer>
</div>
</body>
</html>
